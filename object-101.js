let info ={
	name :'ashish kumar',
	age :20,
	location :'Mysore,India'
}
console.log(`${info.name} is ${info.age} and lives in ${info.location}`)

info.age=19;
console.log(`${info.name} is ${info.age} and lives in ${info.location}`)